on mac the recording for loopback and on its microphone are always coming back empty 

mac is no longer sending audio into the connection with both nano cortex device and focusrite scarlet, probably linked to the above 

windows is fine so must be bug on mac side, i wonder if its something to do with the output selector and the channels conflictin? metronome standalone works ok on mac and so does TCP control, so session is still live

mac permission issue perhaps? terminal has permission to microphone but our app hasnt requested it.




